# Production Environment Variables for Frontend
# Copy this to .env.production.local and fill in your production values

# API URL - Must use HTTPS in production to avoid mixed content errors
# Use the /api path since Nginx proxies it to the backend
NEXT_PUBLIC_API_URL=https://yourdomain.com/api

# IMPORTANT: Never use http:// in production - it will cause mixed content errors
# after SSL is enabled. All URLs must use https://

# App Configuration
NEXT_PUBLIC_APP_ENV=production
NEXT_PUBLIC_APP_NAME=Baitech
NEXT_PUBLIC_APP_DESCRIPTION=Premium technology products and professional tech services in Kenya

# Analytics (Optional - add your tracking IDs)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX

# Site URL - Must use HTTPS
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
